<% provide(:title, 'All users') %>
<h1>All Users</h1>

<ul class = "users">
  <% @users.each do |user| %>
   <% render user %>
    
  <% end %>
</ul>
<table>
  <tbody>
     <tr>
     </tr>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path %>
